sudo: required
services:
  - docker

before_install:
  - docker build -t vernseaw/docker-react-example -f Dockerfile.dev .

script:
  - docker run vernseaw/docker-react-example npm run test -- --coverage
